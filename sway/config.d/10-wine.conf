# Hide Wine's floating system tray (explorer.exe with empty title)
# It's needed for tray icons but the floating window is noise on Wayland.
# Send it to scratchpad — Wine apps still work, summon with $mod+minus if needed.
for_window [class="explorer.exe" title="^$"] move scratchpad
